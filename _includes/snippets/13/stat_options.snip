* `-l select=<nnodes>` - how many nodes does your job need? Note that there are 24 cores per node on ARCHER.

* `-l walltime=<hours:minutes:seconds>` - How much real-world time (walltime) will your job take to
  run?
